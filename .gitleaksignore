# Reviewed synthetic derived-hex equivalence identifiers in the immutable alpha.3 source history.
f3df9a21931d4dc6d0912e71c8507612a2cab104:cli/internal/embeddedcorpusv6/data/corpus-index.json:generic-api-key:1

# CI gitleaks scan runs against the working tree (gitleaks dir), not git history,
# so these fingerprints omit the commit prefix. Reviewed 2026-09-23.
#
# Synthetic "derived-<hex>" equivalenceKey values in the minified test corpus fixture;
# not real secrets, just deterministic hex identifiers generated for test scenarios.
# All 17 hits on this file collapse to one fingerprint because gitleaks reports the
# minified single-line JSON as line 1 for every match.
testdata/synthetic-cluster-scenarios/v4/corpus-index.json:generic-api-key:1

# kubectl resource argument "apiservices.apiregistration.k8s.io" flagged by the
# generic-api-key heuristic; it is a Kubernetes API group name, not a credential.
cli/internal/localcollector/collect.go:generic-api-key:65
